# dice

Game-specific frontend boundary. Keep UI, animation, view state and game API adapters for dice isolated from other games.

Current implementation is wired from `src/screens/DiceScreen.tsx` and the corresponding `gamesApi.dice` namespace.