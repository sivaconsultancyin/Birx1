# aviator

Game-specific frontend boundary. Keep UI, animation, view state and game API adapters for aviator isolated from other games.

Current implementation is wired from `src/screens/AviatorScreen.tsx` and the corresponding `gamesApi.aviator` namespace.